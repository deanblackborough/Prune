#pragma once

#include "prune/scene/sandbox_scene.hpp"

namespace prune {

    class Options {
    public:
        void draw(SceneOptions& scene_options, GridOptions& grid_options);
    private:

    };
}